apiVersion: v1
kind: Service
metadata:
  name: openclaw-gateway
  namespace: shengsuanyun
  labels:
    app: openclaw-gateway
spec:
  type: ClusterIP
  ports:
  - port: 18789
    targetPort: 18789
    protocol: TCP
    name: gateway
  selector:
    app: openclaw-gateway
